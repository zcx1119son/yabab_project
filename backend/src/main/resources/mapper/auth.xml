<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "fs.human.yabab.auth.dao.AuthDAO">

    <insert id="insertUser" parameterType="fs.human.yabab.auth.vo.UserVO">
        INSERT INTO TB_USER (
            USER_ID,
            USER_NAME,
            USER_NICKNAME,
            USER_EMAIL,
            USER_PASSWORD,
            USER_PHONE,
            USER_ROLE,
            USER_IMAGE_PATH,
            USER_IMAGE_NAME,
            USER_JOINDATE,
            USER_FAVORITE_TEAM,
            CREATED_DATE,
            CREATED_BY,
            UPDATED_DATE,
            UPDATED_BY
        ) VALUES (
            #{userId},
            #{userName},
            #{userNickname},
            #{userEmail},
            #{userPassword},
            #{userPhone},
            #{userRole},
            #{userImagePath, jdbcType=VARCHAR},
            #{userImageName, jdbcType=VARCHAR},
            SYSDATE,
            #{userFavoriteTeam, jdbcType=VARCHAR},
            SYSDATE,
            #{userEmail},
            NULL,
            NULL
        )
    </insert>

    <!-- 아이디 중복 확인 -->
    <select id="countUserId" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM TB_USER WHERE USER_ID = #{userId}
    </select>

    <!-- 닉네임 중복 확인 -->
    <select id="countUserNickname" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM TB_USER WHERE USER_NICKNAME = #{userNickname}
    </select>

    <select id="countVerifiedTokenByEmail" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM TB_RESET_TOKEN WHERE TARGET_MAIL = #{email}
    </select>

    <!-- 사용자 인증 (로그인)-->
    <select id="findUserByIdAndPw" resultType="UserVO">
        SELECT * FROM TB_USER WHERE USER_ID = #{userId} AND USER_PASSWORD = #{userPassword} AND USER_DELETED_FLAG = 0
    </select>

    <!-- 아이디 찾기 -->
    <select id="findUserIdByNameAndEmail" resultType="string">
        SELECT USER_ID FROM TB_USER WHERE USER_NAME = #{userName} AND USER_EMAIL = #{userEmail}
    </select>

    <!-- 유저 존재 확인 (비밀번호 찾기) -->
    <select id="countUserByIdAndEmail" resultType="int">
        SELECT COUNT(*) FROM TB_USER WHERE USER_ID = #{userId} AND USER_EMAIL = #{userEmail}
    </select>

    <!-- 비밀번호 업데이트 -->
    <update id="updateUserPassword" parameterType="fs.human.yabab.auth.vo.UserVO">
        UPDATE TB_USER SET USER_PASSWORD = #{userPassword} WHERE USER_ID = #{userId} AND USER_EMAIL = #{userEmail}
    </update>

</mapper>